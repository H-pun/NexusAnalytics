[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
module-name = "analytics_rust_core"
binding = "pyo3"
# Note: extension-module feature is automatically handled by crate-type = ["cdylib"] in Cargo.toml

